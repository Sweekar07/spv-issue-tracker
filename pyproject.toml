[project]
name = "spv-issue-tracker"
version = "0.1.0"
description = "Django Backend Service for tracking issues using UV, Docker and Postgres"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.10",
    "django-environ>=0.12.0",
    "psycopg[binary]>=3.3.2",
]
